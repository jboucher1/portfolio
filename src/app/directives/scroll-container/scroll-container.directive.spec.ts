import { ScrollContainerDirective } from './scroll-container.directive';

describe('ScrollContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollContainerDirective();
    expect(directive).toBeTruthy();
  });
});
